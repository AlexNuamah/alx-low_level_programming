#include <stdio.h>

/**
 * main - The main function is the starting point of execution for the program
 * Description:
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
