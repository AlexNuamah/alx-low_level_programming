#include <stdio.h>

/**
 * main - The main function is the starting of the execution for the program
 * Description:
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar(i + '0');
	}
	putchar('\n');
	return (0);
}
